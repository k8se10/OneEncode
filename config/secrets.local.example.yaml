# Copy this file to secrets.local.yaml and fill in real destination URLs/stream keys.
# secrets.local.yaml is gitignored — never commit real values.
# Keys here must match each rtmp-push leg's `destinationUrlEnv` in legs.local.yaml.

ONEENCODE_PLATFORM_A_URL: "rtmp://REPLACE_WITH_REAL_INGEST_HOST/live/REPLACE_WITH_REAL_STREAM_KEY"
ONEENCODE_PLATFORM_B_URL: "rtmp://REPLACE_WITH_REAL_INGEST_HOST/live/REPLACE_WITH_REAL_STREAM_KEY"
